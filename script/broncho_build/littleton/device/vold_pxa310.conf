## vold configuration file for the emulator/SDK

volume_sdcard {
    emu_media_path /class/mmc_host/

    media_type     mmc
    mount_point    /sdcard
	ums_path devices/platform/pxa3xx-u2d/gadget/lun0
}
